# Mage Fire

Fire school spells.

## Line highlights

None specific to this guild.

## Shortcuts

### Spells

| Shortcut | Full name | Notes |
|----------|-----------|-------|
| `cfa` | Flame arrow | `cast_spell` targeting. |
| `cf` | Firebolt | `cast_spell`. |
| `cfb` | Fire blast | `cast_spell`. |
| `cflshield` | Flame shield | No args: `cast flame shield at me`. With args: at target (unquoted `at`). |
| `chred` | Heat reduction | `cast_spell`. |
| `clvb` | Lava blast | `cast_spell`. |
| `clvs` | Lava storm | `cast_spell`. |
| `cmetb` | Meteor blast | `cast_spell`. |
| `cmets` | Meteor swarm | `cast_spell`. |
| `cserf` | Searing fervor | `cast_spell`. |

!!! note "Overlap"
    **`cfa`** may clash with **Kharim** flame arrow — guild order decides.

## Profile

Enable **`mage_fire`** in **`/guilds`**.
