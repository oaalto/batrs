# Ranger

Bladed fury shortcut, a one-line “pull / engage” opener, and torch creation. No guild-only line rules.

## Line highlights

None from this guild.

## Shortcuts

### Skills

| Shortcut | Full name | Notes |
|----------|-----------|-------|
| `ubf` | Bladed fury | Optional target (standard `use` rules). |
| `utc` | Torch creation | Sends `use 'torch creation'`. |

### Other

| Shortcut | Full name | Notes |
|----------|-----------|-------|
| `cs` | Open fight | Target required: `target`, `use 'bladed fury'`, then `@k` on that target. |

!!! note "Shared shortcut"
    **`cs`** overlaps **Monk kiai**, **Tzarakk** charge, **Riftwalker** spark opener.

## Profile

Enable **`ranger`** in **`/guilds`**.
