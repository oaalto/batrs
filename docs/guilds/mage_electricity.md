# Mage Electricity

Lightning school. **`clb`** here is **lightning bolt** (not the acid spell).

## Line highlights

None specific to this guild.

## Shortcuts

### Spells

| Shortcut | Full name | Notes |
|----------|-----------|-------|
| `csg` | Shocking grasp | `cast_spell`. |
| `clb` | Lightning bolt | `cast_spell` (**not** acid **lock biter** — different guild). |
| `cbl` | Blast lightning | `cast_spell`. |
| `cfl` | Forked lightning | `cast_spell`. |
| `ce` | Electrocution | `cast_spell`. |
| `ccl` | Chain lightning | `cast_spell`. |
| `cls` | Lightning storm | `cast_spell`. |
| `cench` | Energy channeling | `cast_spell`. |
| `cltshield` | Lightning shield | No args: `cast lightning shield at me`. With args: at target. |
| `cmaglev` | Magnetic levitation | `cast_spell`. |

## Profile

Enable **`mage_electricity`** in **`/guilds`**.
